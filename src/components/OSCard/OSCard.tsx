import React from "react";

interface OSCardProps {
  title: string;
  personalisedStyle?: any;
  onClick: () => void;
  disabled?: boolean;
}

const OSCard: React.FC<OSCardProps> = (props: OSCardProps) => {
  return <></>;
};

export default OSCard;
